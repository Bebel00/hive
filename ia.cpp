#include "ia.h"

void IA::appliquer_move(){
    if(TypeAction == TypeAction::PLACE){

    }
    else if(TypeAction == TypeAction::MOVE){

    }
    else{
        cout << "type d'action inconnue";
    }
}
